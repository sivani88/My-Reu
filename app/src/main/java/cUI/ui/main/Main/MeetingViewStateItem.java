package cUI.ui.main.Main;

public class MeetingViewStateItem {
   /* private final long id;
    @NonNull
    private final String mName;

    @NonNull
    private  final String subject;
    @NonNull
    private  final  String mMail;
    @NonNull
    private final String avatarUrl;
    @NonNull
    private final String mDate;
    @NonNull
    private final String mHour;

    public MeetingViewStateItem(long id, @NonNull String name, @NonNull String subject, @NonNull String mail, @NonNull String avatarUrl, @NonNull String date, @NonNull String hour) {
        this.id = id;
        mName = name;
        this.subject = subject;
        mMail = mail;
        this.avatarUrl = avatarUrl;
        mDate = date;
        mHour = hour;
    }

    public long getId() {
        return id;
    }

    @NonNull
    public String getName() {
        return mName;
    }

    @NonNull
    public String getSubject() {
        return subject;
    }

    @NonNull
    public String getMail() {
        return mMail;
    }

    @NonNull
    public String getAvatarUrl() {
        return avatarUrl;
    }

    @NonNull
    public String getDate() {
        return mDate;
    }

    @NonNull
    public String getHour() {
        return mHour;
    }


    @Override
    public boolean equals(@Nullable Object obj) {
        if (this == obj) return  true;
        if(obj == null || getClass() != obj.getClass()) return  false;
        MeetingViewStateItem that = (MeetingViewStateItem) obj;
        return  id == that.id && mName.equals(that.mName) && subject.equals(that.subject)
                && mMail.equals(that.mMail) && avatarUrl.equals(that.avatarUrl) && mDate.equals(that.mDate) && mHour.equals(that.mHour);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id, mName, subject, mMail, avatarUrl, mDate, mHour);
    }

    @NonNull

    @Override
    public String toString() {
        return "MeetingViewStateItem{" +
                "id=" + id +
                ", mName='" + mName + '\'' +
                ", subject='" + subject + '\'' +
                ", mMail='" + mMail + '\'' +
                ", avatarUrl='" + avatarUrl + '\'' +
                ", mDate='" + mDate + '\'' +
                ", mHour='" + mHour + '\'' +
                '}';
    }*/
}
